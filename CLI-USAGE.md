# استخدام Vercel CLI المحلي

## تم تثبيت Vercel CLI محليًا في المشروع

الآن يمكنك استخدام Vercel CLI مباشرة من خلال npm scripts بدلاً من التثبيت العالمي.

## الأوامر المتاحة

### 1. تشغيل Vercel CLI مباشرة
```bash
npm run vercel
```

### 2. تسجيل الدخول إلى Vercel
```bash
npm run vercel:login
```

### 3. النشر إلى الإنتاج
```bash
npm run vercel:deploy
```

### 4. عرض قائمة المشاريع
```bash
npm run vercel:projects
```

## استخدام setup-vercel.sh

تم تحديث سكريبت الإعداد ليستخدم CLI المحلي:

```bash
./setup-vercel.sh
```

السكريبت الآن:
- ✅ يتحقق من تثبيت Vercel CLI محليًا
- ✅ يقوم بتشغيل npm install إذا لم يكن مثبتًا
- ✅ يستخدم npm scripts بدلاً من الأوامر العامة

## المزايا

- 🎯 **إصدار محدد**: يضمن استخدام نفس الإصدار في كل مكان
- 🔒 **أمان أفضل**: لا حاجة لصلاحيات المدير (sudo)
- 📦 **سهولة الإدارة**: يُدار كجزء من dependencies المشروع
- 🔄 **تناسق**: نفس الإصدار لجميع المطورين

## أمثلة الاستخدام

### نشر المشروع
```bash
npm run vercel:deploy
```

### التحقق من الإصدار
```bash
npm run vercel -- --version
```

### الحصول على مساعدة
```bash
npm run vercel -- --help
```

## ملاحظات

- كل أوامر Vercel CLI متاحة من خلال `npm run vercel -- <command>`
- يمكنك إضافة flags بعد `--` مثل: `npm run vercel -- dev --debug`
- الإصدار المثبت حاليًا: 49.1.1

